{% extends 'GeoPagosCommonBackBundle:Layout:layout.html.twig' %}

{% block content %}
    <div class="row">
        {% include 'GeoPagosCommonBackBundle:Common:messages.html.twig' %}
        {{ form_errors(form,{'flashMessage':1}) }}

        {{ form_start(form, {
            'action': path('common_back_withholding_tax_dynamic_rule_province_rate_new', {'withholdingTaxRuleId': withholdingTaxRule.id}),
            'attr': {'class': 'form-horizontal', 'id': 'wt_rule'
            }}) }}
        <div class="col-md-8">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h2><strong>{{ 'withholding_tax.dynamic_rule_province_rate.new.title'|trans }}</strong></h2>
                </div>

                <div class="panel-body">
                    {% include 'GeoPagosWithholdingTaxBundle:WithholdingTaxDynamicRuleProvinceRate:body.html.twig' %}
                </div>
                {{ form_end(form) }}

                {% include 'GeoPagosWithholdingTaxBundle:WithholdingTaxDynamicRuleProvinceRate:edit_buttons.html.twig' with {
                    'submitLabel':'merchant.buttons.save.new.title'|trans
                } %}

            </div>
        </div>
    </div>
{% endblock %}